DB_PATH=data/prompts.json
DEFAULT_THEME=light

# ------------------------------------------
# 🤖 OpenAI API Schlüssel
# ------------------------------------------
# Hier den OpenAI API-Key eintragen (z. B. sk-...)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ------------------------------------------
# Fetcher Einstellungen
# ------------------------------------------
# NEU (für Medium & optional Auth)
# 1) Medium-Login-Cookies als kompletter Cookie-Header (nur eigene, legitime Nutzung)
MEDIUM_COOKIE=

# 2) Optional: Basic-Auth für Seiten mit htaccess (falls gebraucht)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# 3) Optional: Weitere Header als JSON-Dict (z. B. zusätzliche Cookies)
# Beispiel: {"X-Requested-With":"XMLHttpRequest","Cookie":"a=b; c=d"}
EXTRA_HEADERS_JSON=

PLAYWRIGHT_TIMEOUT_MS=25000
PLAYWRIGHT_NAV_TIMEOUT_MS=25000
